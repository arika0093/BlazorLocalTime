@namespace BlazorLocalTime

@if (ChildContent != null && LocalTimeService.IsTimeZoneInfoAvailable)
{
    @ChildContent(LocalTimeService.GetBrowserTimeZone())
}
@if (UnknownContent != null && !LocalTimeService.IsTimeZoneInfoAvailable)
{
    @UnknownContent
}
