@namespace BlazorLocalTime

@if (ChildContent != null && LocalTimeService.IsLocalTimeZoneSet)
{
    @ChildContent(LocalTimeService.GetBrowserTimeZone())
}
@if (UnknownContent != null && !LocalTimeService.IsLocalTimeZoneSet)
{
    @UnknownContent
}
